import React from "react";
import Calendar from "react-calendar";


function Calendar() {
    const date = (Calendar.map({date

    }))

    return (
        <div className="pretty-calendar">

        </div>
    )
}

export default Calendar;